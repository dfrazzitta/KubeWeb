@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers



<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item dropdown">
            @*<a class="nav-link text-dark bg-opacity-10"   asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity?.Name!</a>
        *@
            <a href="#" class="nav-link dropdown-toggle text-danger" data-bs-toggle="dropdown">@User.Identity?.Name! Admin</a>
            <div class="dropdown-menu dropdown-menu-end">
                @*<a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>*@
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Logout">Logout</a>

                <div class="dropdown-divider"></div>
                @* <a href="#" class="dropdown-item">Logout</a>  *@
            </div>
        </li>
        <li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                @* <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button> *@
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            @*    <a class="nav-link text-dark bg-light"   asp-area="Identity" asp-page="/Account/Register">Register</a>
        *@
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>
    }
</ul>
